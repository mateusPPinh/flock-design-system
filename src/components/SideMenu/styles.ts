import styled from 'styled-components';

const SideMenuComponent = styled.div``;

const SideMenuContainerClosed = styled.div``;

const Title = styled.div`
  display: flex;
  align-items: center;
`;

export { SideMenuComponent, SideMenuContainerClosed, Title };